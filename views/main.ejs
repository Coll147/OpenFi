<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <title><%= title %></title>
  <link rel="stylesheet" href="/styles/main-default.css">
  <link rel="icon" type="image/x-icon" href="./assets/favicon.ico">
  <script src="/scripts/global.js"></script>

</head>
<body class="layout">

  <!-- navbar -->
  <%- include ('partials/navbar') %>

  <!-- contenido -->
  <main id="content">
    <%- body -%>
  </main>

  <script>
    document.querySelectorAll('.navbar__menu li').forEach(li => {
      li.addEventListener('click', () => {
        window.location.href = `/dashboard/${li.dataset.page}`;
      });
    });

    const path = window.location.pathname;
    const currentPage = path.split('/').filter(Boolean).pop();
    document.querySelectorAll('.navbar__menu li').forEach(item => {
      if (item.dataset.page === currentPage) {
        item.classList.add('active');
      }
    });
  </script>

</body>
</html>
